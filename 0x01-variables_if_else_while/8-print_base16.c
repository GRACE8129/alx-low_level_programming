#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (successful)
 */

int main(void)
{
int n;
int m;

for (n = '0'; n <= '9'; n++)
putchar(n);
for (m = 'a'; m <= 'f'; n++)
putchar(m);
putchar('\n');
return (0);
}
